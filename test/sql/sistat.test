# name: test/sql/sistat.test
# description: Test sistat extension
# group: [sql]

# Before we load the extension, this will fail
statement error
SELECT * FROM SISTAT_Tables();
----
Catalog Error: Table Function with name sistat_tables does not exist! Did you mean "histogram_values"?  LINE 1: SELECT * FROM SISTAT_Tables();                       ^ ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

# Load the extension
load __TEST_BUILD_LOCATION__/sistat

# Confirm it loaded by checking if the function exists
statement ok
SELECT * FROM SISTAT_Tables() WHERE 1=0;
